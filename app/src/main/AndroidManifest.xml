<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.panosdim.moneytrack">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_moneytrack"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".income.IncomeDetails"
            android:label="@string/title_activity_income_details"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".expense.ExpenseDetails"
            android:label="@string/title_activity_expense_details"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".category.CategoryDetails"
            android:label="@string/title_activity_category_details"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".income.FilterIncome"
            android:label="@string/filter_income"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".expense.FilterExpenses"
            android:label="@string/filter_expenses"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>